# GENERATED FILE -- do not edit.
# Rendered by wallust from templates/hyprlock-colors.conf.
# Sourced by hypr/hyprlock.conf.
#
# hyprlock is still hyprlang (only the COMPOSITOR moved to Lua in 0.55+), so
# these are hyprlang $variables.
#
# Values are pre-composed per use-site rather than exposed as bare colours,
# because hyprlang variable expansion cannot build a colour from parts --
# "$cyan80" would be parsed as a variable named cyan80, not $cyan + "80".
# Translucent values therefore use the 8-digit rgba(RRGGBBAA) form, which
# hyprlang parses unambiguously (0.5 alpha == 0x80).

# Gradient borders: profile image and the input field outline.
$lockGradient = rgb({{color2 | strip | lower}}) rgb({{color1 | strip | lower}}) rgb({{color3 | strip | lower}})

# Input field interior and text.
$lockInner = rgba({{color0 | strip | lower}}b3)
$lockFont  = rgb({{color15 | strip | lower}})

# Auth feedback.
$lockCheck = rgb({{color3 | strip | lower}}) rgb({{color2 | strip | lower}}) rgb({{color5 | strip | lower}})
$lockFail  = rgb({{color1 | strip | lower}})
$lockCaps  = rgba({{color1 | strip | lower}}80) rgba({{color3 | strip | lower}}80)
$lockBoth  = rgba({{color2 | strip | lower}}80) rgba({{color5 | strip | lower}}80)

# Labels (clock, username) and their glow.
$lockText  = rgba({{color7 | strip | lower}}ff)
$lockGlow  = rgba({{color2 | strip | lower}}80)

# Neutral drop shadow behind the profile image.
$lockShadow = rgba(00000080)
